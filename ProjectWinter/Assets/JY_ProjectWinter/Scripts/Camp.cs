using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Pun;

public class Camp : MonoBehaviourPun
{
    public CampFirePlace campFirePlace;       // 산장 벽난로 스크립트   
    

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
